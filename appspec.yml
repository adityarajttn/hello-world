version: 0.0
os: linux
files:
  - source: /
    destination: /opt/tomcat/webapps/

hooks:
ApplicationStart:
  ApplicationStart:
    – location: tomcat_start.sh
      timeout: 300
      runas: root

